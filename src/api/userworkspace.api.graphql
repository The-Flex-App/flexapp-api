extend type Query {
  getUserWorkSpaces(workspaceId: String!): UserWorkspace!
  userWorkspaceInfoByRole(role: String!):UserWorkspace!
}

type UserWorkspace {
  id: String!
  userId: String!
  workspaceId: String!
  createdAt: DateTime!
  updatedAt: DateTime!
  role: String!
}

