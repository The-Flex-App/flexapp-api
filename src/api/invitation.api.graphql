extend type Query {
  invitationById(id: ID!): Invitation!
}

extend type Mutation {
  createInvitaton(input: InvitationInput!): invitationId: ID!
  deleteInvitation(id: ID!): Invitation
}

type Invitation {
  id: ID!
  createdAt: DateTime!
  updatedAt: DateTime!
  expiryDate: DateTime!
  userId: String
  workspaceId: String!
}

input InvitationInput {
  workspaceId: String!
  userId: String!
}
